<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.spendwithbrain">

    <application
        android:name=".app.ExpensesApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".screens.spalsh.SplashActivity"
            android:configChanges="orientation"
            android:screenOrientation="portrait"
            android:theme="@style/NoActionBarNoFullScreen"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".screens.login.LoginActivity"
            android:configChanges="orientation"
            android:screenOrientation="portrait"
            android:theme="@style/NoActionBarNoFullScreen"
            android:windowSoftInputMode="adjustResize"/>
        <activity android:name=".screens.register.RegisterActivity"
            android:configChanges="orientation"
            android:screenOrientation="portrait"
            android:theme="@style/NoActionBarNoFullScreen"
            android:windowSoftInputMode="adjustResize"/>
        <activity android:name=".screens.main.MainActivity"
            android:configChanges="orientation"
            android:screenOrientation="portrait"
            android:theme="@style/NoActionBarNoFullScreen"
            android:windowSoftInputMode="adjustResize"/>
        <activity android:name=".screens.converter.ConverterActivity" />
        <activity android:name=".screens.addaction.AddActionActivity" />
    </application>

</manifest>